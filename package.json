{
  "name": "company-theme",
  "version": "1.0.0",
  "dependencies": {
    "@compodoc/live-server": "^1.2.3",
    "htmlhint": "^1.1.4",
    "npm-watch": "^0.11.0",
    "prettier": "2.7.1"
  },
  "watch": {
    "dev": {
      "patterns": [
        "site/**/*.*",
        "components/Styling/styles/*.*",
        "components/Styling/scripts/*.*",
        "tailwind.config.js",
        "components/Resources/*.*"
      ]
    }
  },
  "scripts": {
    "prepare": "npm install --prefix components/Styling",
    "start": "npm run watch dev & live-server public --no-browser -q",
    "watch": "npm-watch",
    "dev": "rm -fR .tmp; htmlhint site/**/*.html; mkdir -p .tmp; cp -a components/Resources/. .tmp; cp -a site/. .tmp;NODE_ENV=development npm run --prefix components/Styling publish -- --output-filename ../../.tmp/js/main.js; rm -fR public; cp -R .tmp public",
    "build": "htmlhint site/**/*.html; rm -fR .public; cp -a components/Resources/. public; cp -a site/. public;NODE_ENV=production npm run --prefix components/Styling publish -- --output-filename ../../public/js/main.js"
  },
  "author": "",
  "license": "ISC"
}
